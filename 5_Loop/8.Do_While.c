#include <stdio.h>

int main(void)
{
    // condition false or true do while run at least once

    short n = 5;
    do
    {
        printf("I will run at least once\n");
    } while (n == 5);

    return 0;
}