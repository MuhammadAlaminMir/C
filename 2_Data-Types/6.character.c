#include <stdio.h>
int main()
{
    // character
    // we have two different character type
    // 1. signed, & 2, unsigned
    // char print format is %c

    // we have assign char value into single cotes
    char a = 'A';
    printf("Char - %c", a);

    return 0;
}